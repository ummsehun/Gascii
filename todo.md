# Gascii TODO (엔터프라이즈 기준, 한국어)

## 목표
- `play.sh` 의존성을 제거하고 Rust 단일 실행 경로(`cargo menu`)로 통합한다.
- 공통 상수/경로를 `src/shared/constants.rs`로 중앙화한다.
- 대형 터미널에서 성능 저하 원인을 계측 가능하게 만든다.

## P0. 아키텍처 정리 (필수)
- [x] `play.sh` 제거
- [x] 메뉴(TUI) -> 재생 런처를 Rust 코드에서 직접 수행
- [x] `cargo menu` 단일 진입점 유지
- [x] ESC/q 즉시 종료 (완전 종료)
- [x] `font-size`를 `Gascii.config`에 즉시 저장

## P1. 상수 중앙화 (`src/shared/constants.rs`)
- [x] 로그 파일명(`error.log`, `debug.log`) 중앙화
- [x] 미디어 경로/확장자(`assets/video`, `assets/audio`, ext 목록) 중앙화
- [x] 메뉴 라벨/로고/기본 폰트값 중앙화
- [x] 윈도우 모드 기본 크기(240x68) 중앙화
- [ ] 남은 하드코딩 문자열(사용자 메시지/성능 임계값) 2차 정리

## P2. 실행 안정성 / 관측성
- [x] `error.log`/`debug.log` 이원화 로거 적용
- [x] 메뉴 선택 -> 런처 -> 재생 경로를 하나의 프로세스로 통합
- [ ] 실행 세션 ID 기반 로그 묶음(메뉴/재생 구간 연계)
- [ ] 치명 오류 시 사용자 메시지 + 복구 가이드 일관화

## P3. 성능 최적화 (대형 터미널 대응)
- [ ] 렌더 루프 프레임 처리시간(P50/P95) 계측
- [ ] 터미널 크기별 권장 해상도 프리셋 자동 적용
- [ ] adaptive downscale 정책 도입
- [ ] 디코더/렌더 큐 백프레셔(큐 길이, drop률) 로그화

## P4. 품질 게이트
- [ ] `cargo menu` 5회 연속 실행 시 런처 실패 0회
- [ ] ESC 종료 시 raw mode 잔류 0회
- [ ] 큰 터미널(예: 200x56+)에서 드롭 원인 로그로 설명 가능
- [ ] 기존 대비 체감 품질(선명도/동기화) 회귀 없음

## 이번 스프린트 결과
- [x] Ratatui TUI 메뉴 + 시작 로고 적용
- [x] `play.sh` 삭제 및 Rust 런처(`src/core/launcher.rs`) 추가
- [x] `shared/constants` 신설 및 핵심 상수 이관
- [ ] 성능 계측/자동 품질 제어는 다음 스프린트에서 진행
